on:
  push:
    branches:
      - main
    paths:
      - 'python-demo/**'
      - '.github/workflows/test.yml'
  workflow_dispatch:

jobs:
    job1:
        runs-on: ubuntu-22.04
        steps:
            - name: テストname
              run: pwd

            - name: テストstep2
              run: ls